/iTraMS/LOGS/*.log {
    missingok
    notifempty
    size 100M
    copytruncate #truncate the original log file in place after creating a copy
    compress
    rotate 10
    dateext
}

30 18 * * * /usr/sbin/logrotate -v /etc/logrotate.d/joblogs

11 11 * * * /usr/sbin/logrotate -v /etc/logrotate.d/joblogs

12 12 * * * /usr/sbin/logrotate -v /etc/logrotate.d/joblogs